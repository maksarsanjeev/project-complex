# Мост SketchUp

Плагин, который живёт **внутри** SketchUp и даёт внешней стороне управлять
моделью по HTTP. Вынести его никуда нельзя: Ruby API работает только в процессе
самого приложения.

## Установка

Скопировать `sketchup_mcp_server.rb` в папку плагинов и перезапустить SketchUp:

```
Windows: %APPDATA%\SketchUp\SketchUp 2024\SketchUp\Plugins\
macOS:   ~/Library/Application Support/SketchUp 2024/SketchUp/Plugins/
```

Состояние — меню `Extensions → MCP Server → Server Status`: покажет порт, pid,
открытую модель и идентификатор экземпляра.

## Несколько окон одновременно (v2.1)

На Windows **каждая модель — отдельный процесс** SketchUp со своим интерпретатором
Ruby, и плагин грузится в каждый. Раньше все дрались за порт `8080`: занимал первый
запущенный, остальные показывали модальное окно и для внешнего мира не существовали.

Проверено на трёх окнах — теперь так:

| окно | порт | pid |
|---|---|---|
| test2 | 8080 | 36552 |
| test1 | 50126 | 29920 |
| test3 | 50127 | 26468 |

- Порт `8080` берётся, только если свободен; иначе ОС выдаёт любой свободный.
  Первое окно ведёт себя как раньше, поэтому старые клиенты не ломаются.
- Слушаем `127.0.0.1`, а не `0.0.0.0`. Мост исполняет произвольный Ruby, и в сети
  ему делать нечего — наружу смотрит агент, у которого есть токен.
- **Никаких модальных окон при запуске.** Диалог останавливает главный поток
  SketchUp, а на нём же работает обработчик очереди — мост от этого немел целиком.
- Лог пишется в файл на процесс: `%TEMP%\sketchup_mcp_<pid>.log`. Общий лог не
  годится — строки от разных окон перемешиваются и часть теряется.

## Визитки экземпляров

Порты теперь произвольные, поэтому угадать, где нужная модель, нельзя. Каждое окно
само сообщает о себе файлом в `%LOCALAPPDATA%\complex\instances\sketchup-<pid>.json`:

```json
{
  "instance_id": "54cfeedc-bab6-4156-afdd-1e7b380e78ee",
  "pid": 26468,
  "port": 50127,
  "host": "127.0.0.1",
  "app": "sketchup",
  "model_title": "test3",
  "model_path": null,
  "model_guid": "8d6aff93-64fe-454c-9630-3bbc0be2238d"
}
```

Визитка освежается раз в две секунды, поэтому сохранение под другим именем видно
без перезапуска. При остановке удаляется; визитки умерших процессов подчищаются
при старте по проверке живости PID.

**Адресация: пользователь → агент → экземпляр (`instance_id`) → документ (`model_guid`).**
Номера порта в ней нет ни на одном уровне, поэтому одинаковые порты на разных
машинах коллизий не создают. PID годится только для проверки живости: он
переиспользуется системой и ничего не доказывает.

## Единицы: наружу всегда миллиметры

Внутри SketchUp длины считаются в дюймах, и это не настраивается. Поэтому перевод
происходит ровно на границе: всё пришедшее от клиента сразу переводится в дюймы,
всё уходящее — обратно в миллиметры. Внутри обработчиков дюймы, снаружи
миллиметры, смешения нет нигде. В ответах, где есть длины, стоит поле `units: "mm"`.

**Не переводятся, потому что безразмерны:** направления и нормали, углы в градусах,
коэффициенты масштабирования, количества, размеры снимка в пикселях.

Два исключения в ферме кровли: уклон задаётся отношением (`"6:12"`), а типоразмер
пиломатериала — названием из каталога (`"2x4"`), у которого фактические размеры
дюймовые по определению стандарта. Длины же — пролёт, шаг, свес — в миллиметрах,
по умолчанию 610 и 300.

## Маршруты

| | | |
|---|---|---|
| `GET` | `/instance/info` | кто я, порт, какая модель открыта |
| `POST` | `/view/screenshot` | снимок вьюпорта, PNG в base64 |
| `GET` | `/model/info` `/model/layers` `/model/materials` `/model/entities` `/model/components` | чтение модели |
| `POST` | `/geometry/face` `/edge` `/group` `/box` `/circle` `/arc` `/polygon` `/pushpull` `/followme` | построение |
| `POST` | `/transform/move` `/rotate` `/scale` | преобразования |
| `POST` | `/components/create` `/components/place` | компоненты |
| `POST` | `/construction/roof_truss` | ферма кровли |
| `POST` | `/ruby/execute` | произвольный Ruby |

## Проверка

При открытых окнах SketchUp:

```bash
curl http://127.0.0.1:8080/instance/info
```

Ответ должен назвать модель именно того окна, чей порт указан. Список всех
открытых окон — по файлам в папке визиток.

## Чего ещё нет

Не перенесены возможности из [`zinin/sketchup-mcp2`](https://github.com/zinin/sketchup-mcp2)
(MIT): булевы операции, скругления и фаски рёбер, столярные соединения. Кодовые базы
неродственные, поэтому это отдельная работа, а не слияние.
