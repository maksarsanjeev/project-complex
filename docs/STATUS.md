# Состояние функций

Срез на 2026-07-25, после этапа 1. Составлено по коду, а не по замыслу.

**Обозначения:** ✅ работает по-настоящему · ◐ частично · ○ заглушка

---

## Вьюпорт

| Функция | | Что есть на самом деле |
|---|---|---|
| Сцена three.js | ✅ | реальный рендер, бесконечный грид, вью-куб, освещение |
| Орбита, зум, панорама | ✅ | ограничений по углу нет, вид держится |
| Вписать в кадр | ✅ | по описанной сфере, отдельно для перспективы и орто |
| Режимы shaded / wire / clay / x-ray | ✅ | полноценные материалы, переключение детерминировано |
| Перспектива ⇄ орто | ✅ | |
| Параметрическая демо-башня | ✅ | геометрия генерируется из параметров: ядро, перекрытия, диагрид, витраж |
| Загрузка glb / gltf / obj / stl | ✅ | настоящие лоадеры, нормализация масштаба, drag&drop в холст |
| Счётчики объектов и треугольников | ✅ | обход реальной сцены с учётом инстансов |
| Выделение части кликом | ◐ | подсвечивает материал и пишет id в HUD; свойств объекта не даёт, с аутлайнером не связано |
| Гизмо трансформации | ○ | объект нельзя двигать, вращать, масштабировать |
| Снап 100 мм | ○ | число в HUD статично, привязки нет |
| Сохранение вида / рендер картинки | ○ | |

## Нодовый редактор

| Функция | | Что есть на самом деле |
|---|---|---|
| Отрисовка графа | ✅ | доменные типы узлов, порты типизированы формой глифа |
| Перетаскивание, связи, удаление | ✅ | |
| Палитра узлов по `Tab` | ✅ | 14 типов узлов |
| Вписать граф | ✅ | автоматически при открытии вкладки |
| Миникарта, зум, контролы | ✅ | |
| Сохранение графа | ◐ | уходит в транспорт, живёт в памяти мока; перезагрузка страницы сбрасывает |
| Редактирование параметров узла | ○ | параметры только отображаются |
| Валидация связей по типам портов | ○ | соединить можно что угодно с чем угодно |
| Выполнение графа | ○ | граф ничего не запускает |

## Чат

| Функция | | Что есть на самом деле |
|---|---|---|
| Лента, роли, время, автопрокрутка | ✅ | |
| Отправка и остановка | ✅ | по отношению к моку |
| Стриминг ответа | ◐ | стриминг настоящий, пословный; текст генерирует мок по шаблону |
| Блоки инструментальных вызовов | ◐ | раскрываются, показывают код, результат и длительность — из фикстур |
| Выбор модели | ◐ | структура списка реальная, но это фикстуры; ключи никуда не подключаются |
| Переключатель API ⇄ CLI | ○ | фильтрует список моделей, на поведение не влияет |
| Вложения-картинки | ○ | отображение готово, добавить нельзя — кнопки нет |
| Слэш-команды | ○ | обещаны в подсказке ввода, не реализованы |

## Сессии и сцена

| Функция | | Что есть на самом деле |
|---|---|---|
| Список сессий, поиск, фильтр | ✅ | |
| Переключение сессии | ✅ | подгружает свой диалог и граф |
| Создание сессии | ◐ | создаётся «Без названия»; переименования и удаления нет |
| Аутлайнер, слои по материалу | ◐ | дерево рисуется из фикстур |
| Видимость и блокировка слоя | ○ | переключается, но на 3D-сцену не влияет — аутлайнер и вьюпорт не связаны |
| Персистентность | ○ | всё в памяти, перезагрузка сбрасывает |

## Правая панель

| Функция | | Что есть на самом деле |
|---|---|---|
| Инспектор параметров башни | ✅ | 6 полей, драг-скраб, ограничение диапазонов, живая перестройка геометрии |
| Инспектор свойств объекта | ○ | имя, тип, материал, позиция, поворот, масштаб — ключи заведены, панели нет |
| Инструменты построения (4 кнопки) | ○ | декоративные, без обработчиков |
| Инструменты изменения (8 кнопок) | ○ | декоративные, без обработчиков |
| Движки: список и статусы | ◐ | фикстуры; «привязать» меняет выбор, но никуда не подключается |
| Экспорт | ◐ | выбор формата и прогресс работают, файл не создаётся |
| База знаний | ◐ | живой поиск с дебаунсом по 4 фикстурам |

## Оболочка

| Функция | | Что есть на самом деле |
|---|---|---|
| Раскладка со сплиттерами | ✅ | тянутся, размеры и свёрнутость переживают перезагрузку |
| Сворачивание панелей и чата | ✅ | |
| Тёмная тема | ✅ | переключатель в топбаре, всё через токены |
| Шрифты и кириллица | ✅ | локальные, без CDN, проверены в uppercase с трекингом |
| Белый лейбл | ◐ | токен `--accent` заведён и используется, UI выбора нет |
| Локализация ru / en | ◐ | словари полные, но **34 ключа из 118 не подключены**: часть подписей захардкожена в JSX в обход правила, переключателя языка нет |

## Бэкенд

| Функция | | Что есть на самом деле |
|---|---|---|
| `Transport` как единая точка ввода-вывода | ✅ | интерфейс в `packages/protocol` + `MockTransport` |
| `WsTransport` и gateway | ○ | |
| MCP-мосты SketchUp / Blender / Rhino | ○ | порты `8080 / 9876 / 9890` живут только в фикстурах |
| MCP базы знаний | ○ | |
| Подключение нейросетей по API | ○ | |
| CLI-агенты (codex / gemini / claude) | ○ | |
| Очередь задач | ○ | статусбар показывает 0/1 по флагу загрузки |
| Авторизация, проекты, мультипользовательность | ○ | |

## Качество

| Функция | | Что есть на самом деле |
|---|---|---|
| `typecheck` и `build` | ✅ | чисто |
| CI на GitHub Actions | ✅ | typecheck + build на каждый push |
| Обработка ошибки транспорта | ◐ | сбой стрима попадает в чат системным сообщением |
| Юнит-тесты | ○ | нет |
| E2E-тесты | ○ | нет; Playwright подключён как MCP-инструмент, но тестов не написано |
