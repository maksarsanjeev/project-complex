import type { ToolDef } from './types.ts'

/**
 * Модель спрашивает пользователя.
 *
 * Единственный инструмент, который ничего не делает с моделью: он делает
 * работу предсказуемой. Там, где раньше приходилось выбирать за человека —
 * в каком окне строить, какой материал взять, сносить ли существующее, —
 * появляется вопрос вместо догадки.
 *
 * Выполняется не в движке, а прямо в цикле разговора: вопрос уходит в чат,
 * ход завершается, ответом становится следующее сообщение пользователя.
 * Поэтому у него нет ни движка в обычном смысле, ни маршрута моста.
 */
export const ASK_TOOL: ToolDef = {
  name: 'ask_user',
  engine: 'sketchup',
  command: '__ask__',
  description:
    'Задать вопрос пользователю и дождаться ответа. Используй, когда выбор меняет результат ' +
    'и угадывать нельзя: в каком окне работать, какой материал, удалять ли существующее. ' +
    'После вызова ЗАВЕРШИ ход — ответ придёт следующим сообщением. ' +
    'Не спрашивай о том, что можно посмотреть инструментами, и не спрашивай по мелочам: ' +
    'вопрос стоит человеку внимания.',
  parameters: {
    type: 'object',
    properties: {
      question: { type: 'string', description: 'Вопрос одним предложением' },
      options: {
        type: 'array',
        description: 'Варианты ответа — станут кнопками. Без них будет обычный ввод.',
        items: { type: 'string' },
      },
    },
    required: ['question'],
  },
}
